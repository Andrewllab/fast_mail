# @package _global_

defaults:
  - roca

tags: [abl_filter]

group_by_config_keys:
  - agent.obs_encoder.t11_tokenizer.spatial_encoder._partial_
  - data.gpu_batch_transforms.t10_jitter.sigma

expected_seeds: 1
