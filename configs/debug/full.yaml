# @package _global_

# runs normal training but with verbosity and with wandb disabled

# disable wandb logging
logger:
  wandb: null

# sets level of all command line loggers to 'DEBUG'
# https://hydra.cc/docs/tutorials/basic/running_your_app/logging/
hydra:
  job_logging:
    root:
      level: DEBUG
