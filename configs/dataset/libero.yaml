defaults:
  - /transforms@dataset.transforms: null

task_suite: libero
task: libero_object

dataset:
  _target_: environments.datasets.libero_dataset.LiberoDataset
  data_directory: ${paths.data_dir}/libero
  embeddings_directory: ${paths.data_dir}/libero/task_embeddings
  task: ${..task}
  obs_dim: 9
  action_dim: 7
  state_dim: 110
  max_len_data: 260
  window_size: 10
  traj_per_task: 10
