# @package _global_

defaults:
  # convert HWC to CHW, convert to float, and rescale
  - /transforms@dataset.dataset.transforms.1_toimage: to_torchimage
  # apply ImageNet normalization
  - /transforms@dataset.dataset.transforms.2_norm: imagenet_norm

  - override /dataset: libero
  - override /agent/obs_encoder/tokenizer: resnet

dataset:
  # set task here
  task: libero_object
