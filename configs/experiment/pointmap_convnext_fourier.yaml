# @package _global_

defaults:
  - /models/spatial_encoder@agent.obs_encoder.t10_image_tokenizer.spatial_encoder: fourier_features

# Below is just the default configuration copied here for convenience. Change as needed.
agent:
  obs_encoder:
    t10_image_tokenizer:
      spatial_encoder:
          cat_cartesian_coordinates: True
          max_wavelength: 4
          min_wavelength: 0.4
          n_wavelengths: 4
