# @package _global_

defaults:
  # convert HWC to CHW, convert to float, and rescale
  - /transforms@data.gpu_batch_transforms.1_to_pcd: to_pointcloud

agent:
  obs_encoder:
    embed_dim: 256
data:
  batch_size: 16  # TODO: set as factor of platform.batch_size

logger:
  wandb:
    tags: ["pointcloud", "pointpatch_grayscale"]
