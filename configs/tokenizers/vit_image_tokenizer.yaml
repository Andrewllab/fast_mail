name: vit_image_tokenizer
_target_: transforms.multiview_image_tokenizer.MultiviewImageTokenizer
_partial_: True

image_encoder:
  _target_: models.vit.vit_encoder
  _partial_: True
  embed_dim: ${..embed_dim}

embed_dim: ${..embed_dim}
shared_encoder: False
