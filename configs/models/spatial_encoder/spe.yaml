name: FourierFeatures
_target_: models.fourier_features.FourierFeatures
_partial_: True  # requires input_dim
n_wavelengths: 16
max_wavelength: 4.0
min_wavelength: 0.02  # double the voxel size of 0.01m used in robocasa pointclouds
learn: False
linear_transform: True
components: sincos
cat_input_to_out: False
