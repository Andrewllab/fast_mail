defaults:
  - robocasa_pnp
  - /segmentations: robocasa_pnp_counter_to_sink
  - _self_

task: kitchen_pnp_counter_to_sink
name: kitchen_pnp_counter_to_sink

dataset:
  subfolders: PnPCounterToSink
  # load_subset: 4

env:
  env:
    env_name: PnPCounterToSink
