defaults:
  - default

task_suite: furniture_bench
task: one_leg
name: 2025-03-30_keyboard

dataset:
  _target_: environments.datasets.furniture_bench_alr_v1.AlrFurnitureBenchDataset
  _partial_: True  # requires transforms at runtime
  root_dir: ${paths.data_dir}/insert_one_leg_alr/keyboard
  action_seq_len: 10
  obs_seq_len: 1

device: cpu  # this dataset is small enough to fit in memory
