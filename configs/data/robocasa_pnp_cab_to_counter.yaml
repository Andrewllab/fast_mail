
defaults:
  - default
  - robocasa

task_suite: robocasa
task: PnPCabToCounter
name: kitchen_pnp

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_pnp/PnPCabToCounter
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: PnPCabToCounter