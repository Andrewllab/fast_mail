defaults:
  - maniskill

task: group_1
name: maniskill_group_1

action_seq_len: 5
obs_seq_len: 1

dataset:
  _target_: environments.datasets.maniskill_group1_dataset.ManiSkillDataset
  root_dir: ${paths.data_dir}/maniskill/group1

env:
  env:
    env_id: LiftPegUpright-v1 # LiftPegUpright-v1, PokeCube-v1, PullCube-v1
    sim_backend: physx_cuda
    sensor_configs:
      base_camera:
        width: 256
        height: 256
