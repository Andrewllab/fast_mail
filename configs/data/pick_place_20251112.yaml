# recorded with real robot at 15fps

defaults:
  - default
  - /cameras/calibration@dataset.extrinsics: 2025-09-16
  - real_robot

task_suite: real
task: pick_and_place
name: pick_place_20251112

action_seq_len: 30
obs_seq_len: 1

dataset:
  _target_: environments.datasets.real_dataset_v3.RealRobotDataset
  root_dir: ${paths.data_dir}/pick_place/pick_place_20251112

env:
  env:
    action_type: joint
    impedance_type: hybrid_joint
