
defaults:
  - default
  - robocasa

task_suite: robocasa
task: PnPStoveToCounter
name: kitchen_pnp

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_pnp/PnPStoveToCounter
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: PnPStoveToCounter