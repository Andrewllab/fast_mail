defaults:
  - default

task_suite: furniture_bench
task: one_leg
name: 2025-03-30_demo

dataset:
  _target_: environments.datasets.furniture_bench_alr.AlrFurnitureBenchDataset
  _partial_: True  # requires transforms at runtime
  root_dir: ${paths.data_dir}/furniture_bench_alr/one_leg
  action_seq_len: 10
  obs_seq_len: 1

device: cpu

eval_mode: null
# env_dataset:
#   _target_: environments.simulations.isaac.IsaacSim
#   scene: 
#   render: False
#   num_episode: 20
#   max_step_per_episode: 600
