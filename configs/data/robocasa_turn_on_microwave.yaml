defaults:
  - default
  - robocasa

task_suite: robocasa
task: TurnOnMicrowave
name: TurnOffMicrowave

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_microwave/TurnOnMicrowave
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: TurnOnMicrowave
