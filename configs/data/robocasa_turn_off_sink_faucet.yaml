
defaults:
  - default
  - robocasa

task_suite: robocasa
task: TurnOffSinkFaucet
name: kitchen_sink

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_sink/TurnOffSinkFaucet
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: TurnOffSinkFaucet