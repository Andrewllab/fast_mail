defaults:
  - robocasa_pnp
  - _self_

task: kitchen_pnp_counter_to_stove
name: kitchen_pnp_counter_to_stove

dataset:
  subfolders: PnPCounterToStove
  # load_subset: 4

env:
  env:
    env_name: PnPCounterToStove
