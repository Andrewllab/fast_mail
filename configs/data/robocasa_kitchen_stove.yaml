
defaults:
  - default
  - robocasa

task_suite: robocasa
task: Stove_TurnOff_TurnOn
name: kitchen_stove

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_stove
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: Stove_TurnOff_TurnOn