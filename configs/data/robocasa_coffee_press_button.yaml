
defaults:
  - default
  - robocasa

task_suite: robocasa
task: CoffeePressButton
name: kitchen_coffee

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_coffee/CoffeePressButton
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: CoffeePressButton