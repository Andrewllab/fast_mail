# recorded with real robot at 15fps

defaults:
  - default
  - /cameras/calibration@dataset.extrinsics: 2025-09-16

task_suite: furniture_bench
task: lamp
name: lamp_20250909

action_seq_len: 60
obs_seq_len: 1

dataset:
  _target_: environments.datasets.insert_screw_real_robot_v2.RealRobotDataset
  root_dir: ${paths.data_dir}/lamp/real_world_teleoperation/lamp_2025-09-09_fs_224x224
  # gpu_preprocess_batch_size: ${platform.fs_preprocess_batch_size}  # TODO: move inside transform
