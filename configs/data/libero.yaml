defaults:
  - default

task_suite: libero
task: ???

dataset:
  _target_: environments.datasets.libero_dataset.LiberoDataset
  _partial_: True  # requires transforms at runtime
  root_dir: ${paths.data_dir}/libero/${..task}
  embeddings_file: ${paths.data_dir}/libero/task_embeddings/${..task}.pkl
  action_seq_len: 10
  obs_seq_len: 1

device: cpu

simulation: null
