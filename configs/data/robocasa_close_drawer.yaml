
defaults:
  - default
  - robocasa

task_suite: robocasa
task: CloseDrawer
name: kitchen_drawer

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_drawer/CloseDrawer
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: CloseDrawer