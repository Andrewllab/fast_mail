
defaults:
  - default
  - robocasa

task_suite: robocasa
task: OpenDoubleDoor
name: kitchen_doors

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_doors/OpenDoubleDoor
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: OpenDoubleDoor