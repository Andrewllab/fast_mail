
defaults:
  - default
  - robocasa

task_suite: robocasa
task: TurnOffMicrowave
name: kitchen_microwave

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_microwave/TurnOffMicrowave
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: TurnOffMicrowave