defaults:
  - env

env_dataset:
  env:
    _target_: environments.real_robot.real_robot_env.RealRobotEnv
    _partial_: True
    robot_arm:
      _target_: environments.real_robot.hardware.hardware_franka.FrankaArm
      # _partial_: True
      name: p4 arm
      ip_address: 141.3.53.154
      port: 50053
    robot_hand:
      _target_: environments.real_robot.hardware.hardware_frankahand.FrankaHand
      name: p4 gripper
      ip_address: 141.3.53.154
      port: 50054
    # discrete_devices:
    #   _target_: environments.real_robot.hardware.hardware_realsense.RealSense.get_devices
    #   amount: 3

  fps: 10  # inference should probably be around 10fps on the real robot
