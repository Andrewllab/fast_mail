
defaults:
  - default
  - robocasa

task_suite: robocasa
task: Microwave_Open_Close
name: kitchen_microwave

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_microwave
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: Microwave_Open_Close