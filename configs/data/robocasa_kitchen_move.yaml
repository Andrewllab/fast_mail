
defaults:
  - default
  - robocasa

task_suite: robocasa
task: kitchen_move
name: kitchen_move

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_move
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: CoffeeServeMug
