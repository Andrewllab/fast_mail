defaults:
  - default

env_dataset:
  _target_: environments.gym_env_dataset.GymEnvDataset
  env: ???
  num_episodes: null  # run infinitely
  action_horizon: 1  # show all environment observations
eval_mode: env

# explicitly set these required values to None
dataset: null
batch_size: null
num_workers: null
