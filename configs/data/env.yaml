defaults:
  - default

env_dataset:
  _target_: environments.gym_env_dataset.GymEnvDataset
  env: ???
  num_episodes: null  # run infinitely
  action_horizon: null  # use all predicted actions
  fps: null  # for real environments, set to inference fps
