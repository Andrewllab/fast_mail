task: null
task_suite: null
batch_size: ${platform.batch_size}
device: disk  # by default, assume it dataset doesn't fit in memory
eval_mode: null  # "env" to validate in the environment (sim or real) or a float to validate on subset of training data
num_workers: ${platform.num_workers}
pin_memory: ${platform.pin_memory}
prefetch_factor: ${platform.prefetch_factor}
