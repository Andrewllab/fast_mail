defaults:
  - robocasa

task: coffee_setup_mug
name: coffee_setup_mug_20250920

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_coffee
  subfolders: CoffeeSetupMug

env:
  env:
    env_name: CoffeeSetupMug
