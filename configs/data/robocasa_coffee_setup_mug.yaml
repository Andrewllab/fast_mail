
defaults:
  - default
  - robocasa

task_suite: robocasa
task: CoffeeSetupMug
name: kitchen_coffee

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_coffee/CoffeeSetupMug
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: CoffeeSetupMug