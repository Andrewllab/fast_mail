# recorded with rate limit 30fps, sim_dt = 0.01, decimation = 5

defaults:
  - default

task_suite: furniture_bench
task: one_leg
name: 2025-04-05_metaquest3

dataset:
  _target_: environments.datasets.furniture_bench_alr_v2.AlrFurnitureBenchDataset
  _partial_: True  # requires transforms at runtime
  root_dir: ${paths.data_dir}/insert_one_leg_alr/meta_quest3/dataset_06.04.25
  action_seq_len: 10
  obs_seq_len: 1

device: cpu  # this dataset is small enough to fit in memory

# env:
#   _target_: environments.simulations.isaac.IsaacSim
#   scene: 
#   render: False
#   num_episode: 20
#   max_step_per_episode: 600
