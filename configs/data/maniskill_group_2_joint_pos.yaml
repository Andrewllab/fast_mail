defaults:
  - maniskill

task: group_2
name: maniskill_group_2

action_seq_len: 30 # actions are longer (motionplanned)
obs_seq_len: 1

dataset:
  _target_: environments.datasets.maniskill_group2_dataset_joint_pos.ManiSkillDataset # contains hand camera
  root_dir: ${paths.data_dir}/maniskill/group2_joint_pos

env:
  env:
    env_id: StackCube-v1 # PegInsertionSide-v1, PlugCharger-v1, StackCube-v1
    sim_backend: physx_cpu
    control_mode: pd_joint_pos
    sensor_configs:
      base_camera:
        width: 256
        height: 256
      hand_camera:
        width: 256
        height: 256
