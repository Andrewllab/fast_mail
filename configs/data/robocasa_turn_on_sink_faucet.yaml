
defaults:
  - default
  - robocasa

task_suite: robocasa
task: TurnOnSinkFaucet
name: kitchen_sink

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_sink/TurnOnSinkFaucet
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: TurnOnSinkFaucet