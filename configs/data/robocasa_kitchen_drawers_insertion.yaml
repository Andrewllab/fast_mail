defaults:
  - robocasa

task: kitchen_drawers_insertion
name: kitchen_drawers_insertion_20250920

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage
  # Opening and closing drawers: OpenDrawer,CloseDrawer
  # Insertion: CoffeeSetupMug,CoffeeServeMug
  subfolders: [OpenDrawer,CloseDrawer,CoffeeSetupMug,CoffeeServeMug]
