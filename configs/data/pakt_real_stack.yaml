# recorded with real robot at 15fps

defaults:
  - default
  - /cameras/calibration@dataset.extrinsics: 2025-09-16
  - real_robot

task: stack
name: stack_20260116

action_seq_len: 30
obs_seq_len: 1

dataset:
  _target_: environments.datasets.real_dataset_v3.RealRobotDataset
  root_dir: ${paths.data_dir}/real/stack/${..name}
  subfolders: null
  # load_subset: 4

env:
  env:
    action_type: cartesian
    impedance_type: cartesian
