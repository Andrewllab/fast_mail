
defaults:
  - default
  - robocasa

task_suite: robocasa
task: CloseDoubleDoor
name: kitchen_doors

dataset:
  root_dir: ${paths.data_dir}/robocasa/single_stage/kitchen_doors/CloseDoubleDoor
  preprocessed_dir: ${paths.preprocessed_dir}/${..name}/base

env_dataset:
  env:
    env_name: CloseDoubleDoor