# recorded with real robot at 15fps

defaults:
  - default
  - /cameras/calibration@dataset.extrinsics: 2025-09-16
  - real_robot

task_suite: real
task: arrange
name: arrange_20251119

action_seq_len: 30
obs_seq_len: 1

dataset:
  _target_: environments.datasets.real_dataset_v3.RealRobotDataset
  root_dir: ${paths.data_dir}/arrange/arrange_20251119
  subfolders: null

env:
  env:
    action_type: joint
    impedance_type: hybrid_joint
