name: warmup_cosine
_target_: lr_schedulers.warmup_cosine_lr_scheduler.WarmupCosineLRScheduler
_partial_: True
# optimizer: ???

peak_lr: ${agent.optimizer.lr}  # peak lr
init_lr: ${div:${agent.optimizer.lr}, 100}  # initial lr
warmup_ratio: 0.03  # fraction of total steps for warmup
# total_steps: ${trainer.max_epochs}
