# @package _global_

defaults:
  - kit_hpc
  - _self_

platform:
  name: bwunicluster3
  num_workers: 20  # gpu_h100 node has 96 cores = 24 cores per GPU

hydra:
  launcher:
    timeout_min: 1440  # 24 hours
    partition: gpu_h100
    gres: gpu:1
    cpus_per_task: 24
