# @package _global_

defaults:
  - kit_hpc
  - _self_

platform:
  name: horeka
  num_workers: 36  # accelerated node has 152 cores = 38 cores per GPU

hydra:
  launcher:
    account: hk-project-p0022253
    timeout_min: 1440  # 24 hours
    partition: accelerated
    gres: gpu:1
    cpus_per_task: 38
